"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ramda_1 = require("ramda");
const clients_1 = require("../../../clients");
const FIELDS_START_INDEX = 2;
exports.default = async (app, _, options) => {
    const fields = options._.slice(FIELDS_START_INDEX);
    const settingsValues = await clients_1.apps
        .getAppSettings(app)
        .then(settings => (fields === null ? settings : ramda_1.path(fields, settings)))
        .then(value => JSON.stringify(value, null, 2));
    console.log(settingsValues);
};
